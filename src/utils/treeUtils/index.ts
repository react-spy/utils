/**
 * 树形数据工具
 * @author SPY
 */

import TreeAgent from "./treeAgent";
import {
    walk,
    toTree,
    sortTree,
} from './utils';

const treeUtils = {
    walk,
    toTree,
    sortTree,
    TreeAgent,
};
export default treeUtils;